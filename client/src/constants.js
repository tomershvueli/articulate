// Typically, this would depend on which environment we're looking at
export const API_URL = "http://localhost:5000/";

// The user ID typically would come from our API after we hit an auth endpoint
// This might even be a token that gets passed to requests so each request can be authorized
export const USER_ID = "tomershvueli";
